#
# Default values are shown. Edit/remove as appropriate.
#

# SPI bus speed (in Hz)
spi-speed: 20000000

# GPIO pin to control power to camera.
power-pin: "GPIO23"

# Socket where thermal camera frames should be sent.
frame-output: "/var/run/lepton-frames"
